---
stages:
  - build
  - publish

variables:
  BASE_IMAGE_TAG: python:3-alpine
  VERSION: 1.0.0

build-job:
  stage: build
  script:
    - docker build --build-arg BASE_IMAGE_TAG=$BASE_IMAGE_TAG --build-arg VERSION=$VERSION -t $CI_REGISTRY_IMAGE:$VERSION .

publish-job:
  stage: publish
  script:
    - echo "$CI_JOB_TOKEN" | docker login --password-stdin -u $CI_REGISTRY_USER $CI_REGISTRY
    - docker push $CI_REGISTRY_IMAGE:$VERSION
...
